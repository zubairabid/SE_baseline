def get_segments(path_to_image):
    # voodoo image segmentation stuff
    # Extracts Individual answers (paragraphs) from a page. Each paragraph
    # should have a number attached to it. Generates n images, where n
    # is the number of answers on the page. Also thresholds etc the images.
    # Each image is saved as <tmp>/i.png

    list_of_segments = [] # paths to the image segements
    return list_of_segments
